[build]
rustflags = [
    "-C", "target-cpu=native",
    # Optional: Explicitly enabling features is usually redundant with native, 
    # but we can keep them if you want to be absolutely sure.
    # "-C", "target-feature=+bmi2,+adx,+aes,+avx2,+fma,+sse4.2" 
]
 